# Waypoint 工作原理

> 本文档解释应用**如何运作**：业务模型、实体关系、工作流、AI 的行为边界。能力清单见 `CURRENT_CAPABILITIES.md`，运行方法见 `README.md`。

## 业务模型：一本客户一本书

Waypoint 的核心隐喻是**共享账本**：整个团队共用一个工作区（`data/workbench.json`），每个客户是账本里独立的一「书」。谁能翻开哪本书，由**名字**决定——每本书记录它的 owner 和团队成员，权限即数据本身，不存在独立的权限表。

这个模型的关键后果：

- **上船规则**：BD 拥有自己创建的客户；SA 要参与一个客户，唯一路径是客户 owner 把 SA 的名字加进该客户的 `team`——「被 owner 拉进客户」就是同事上船的方式。Manager 天生全景可见（只读）。confidential 客户对以上所有人之外的角色完全隐身。
- **名字即身份**：Non-Core Member（外部支持者、其他部门同事）没有账号，只作为名字出现在记录里——他们可以被指派完成一个 Next Step，但永远登录不进来。这是有意的最小权限设计。
- **审计**：谁在什么时候动了什么，落在 `audit` 行里；历史不可删除。

## 核心实体与关系

```
User ──(role: admin/manager/bd/sa)
  │
Customer（客户书）
  ├── owner: User（名字）          ← 所有权即可见性
  ├── team[]: User（名字）          ← 被拉上船的同事
  ├── confidential?: true          ← 对无权者隐身 + 数据永不进模型
  ├── Pains[]（痛点）
  ├── Contacts[]（联系人）
  ├── Opportunities[]（机会）── stage / 金额 / 来源
  │      ├── Steps[]（Next Step）── owner + tracker 两向跟踪
  │      └── ↔ Interactions[]（交互记录 ↔ 机会：多对多链接）
  ├── Notes[]（笔记，可含会议纪要原文）
  └── Timeline（时间线：以上全部事件的追加式投影，不可删除）

MOM 行（会议纪要提取产物）
  ── 勾选确认后落账为 → Pain / Contact / Opportunity / Next Step
  ── 关系可追溯：从落账记录能回到它来自的那次纪要（step/opp 携带来源会议 id）
  ── digest（≤220 字）：纪要的一句话摘要，专供时间线一行展示

Product Reference（产品目录，149 服务）
  ── AI 提及产品的唯一合法词表；by[] 记录谁在跟（Product SA 以名字参与）
```

要点：

- **Next Step 的两向跟踪**：一步一个 owner（做这件事的人）+ 一个 tracker（盯着这件事的人）。两角色由服务端强制校验。
- **Timeline 不是独立表**：它是客户全部事件的有序视图——因为源记录不可删除、只追加，时间线天然成立。
- **MOM 是翻译器不是记录器**：纪要原文进 Notes；AI 从原文提取候选行；人勾选后候选行才变成正式实体。

## 主要工作流

### BD 的一天
登录 → Today 屏看焦点（逾期的、到期的、等客户的）→ palette 问「what is waiting on a customer?」（规则直答，零模型）→ 打开客户书 → 记一条 Interaction → 建/推进 Next Step → 离开。

### MOM 落账（会议后）
客户书 → 粘贴纪要原文 → 「Extract」→ AI 返回结构化清单（每行是一个候选：痛点/联系人/机会线索/下一步）→ **逐项勾选** → 确认 → 勾选的行落账为正式记录，未勾选的丢弃。

### 机会发现
客户简报（AI）提示机会线索 → BD 判断 → 主动创建 Opportunity（AI **不会**自动创建）→ 机会页用 AI 分析（每条风险带解释）→ Next Step 推进。

### SA 协作
被 owner 拉进客户 team → 登录即见该书 → 记录技术侧 Interaction / 完成被指派的 Next Step → 在产品目录标记自己跟的产品线。

### Admin 治理
创建用户、设角色 → 配置 AI 连接（或用 `.env`）→ 设预算 → （必要时）重置密码（`npm run set-password`）。

### Manager 监督
登录 → pipeline 总览（只读）：全部客户的机会分布、阶段、金额 → 无任何写入口。

## AI 的行为边界（设计原则）

Waypoint 的 AI 是**被按压才说话的助手**，不是自主 agent。边界全部由服务端强制，不依赖模型自觉：

1. **显式触发**：只有五个人工入口（My Work 聚焦 / 客户简报 / 机会分析 / MOM 提取 / palette 问答）。没有后台扫描、没有定时任务、没有自主行动。
2. **权限先于检索**：服务端装配模型上下文，只装调用者本人可见的数据——模型根本**收不到**它无权看的东西。confidential 客户的数据在快门处就被 403 拦下，永不出门。
3. **常规逻辑不劳烦模型**：等客户/进展/停滞/跟进缺失这类问题由应用规则直接回答，零 token。
4. **输出先进草稿**：任何 AI 输出都是候选行/草稿，**只有人的确认按压才写书**。保存走正常 rev 链（乐观锁 + 原子写）。
5. **不发明事实**：产品只能来自 Product Reference；不发明客户、人、机会、需求、商业事实；数据不够时如实说「记录里没有」。
6. **不静默失败**：模型超时/出错/没连上，界面有明确文案；任务中心保留失败态可重试。
7. **防重复**：处理中的任务，同键再按不产生第二次模型请求（协议层返回同一 taskId）。
8. **成本可控**：Admin 设预算上限；超时 90 秒硬上限；强制使用 fast 模型。
9. **端点可换**：模型配置按「环境变量 → `data/ai.json`」顺序取，Admin 屏可在线改端点且动作写审计；兼容推理型模型（返回 `reasoning_content` 时，推理链与答复分别展示）。

## 重要业务规则（速查）

| 规则 | 含义 |
|---|---|
| 名字即权界 | 客户可见性 = owner / team / owners 名字匹配，别无他路 |
| confidential 墙 | 无权者不可见；数据永不送模型；其余行为完全一样 |
| Timeline 只增不删 | 历史是账本，不是橡皮擦 |
| health 不裸奔 | 健康状态词永远伴随原因出现 |
| 评分必解释 | 不出无解释的评分 |
| 机会不自动建 | AI 只提示线索，创建永远是人手的动作 |
| 产品词表唯一 | Reference 是产品的唯一合法来源 |
| 保存原子 + 合并 | 断电不坏账；并发保存走 rev 合并而非覆盖 |
| 记住设备=会话级承诺 | remember token 登出即双侧吊销（浏览器清除 + 服务器删除 session）；服务器重启亦全部失效 |
| 换人即清扫 | 任何身份切换（登出/换账号/Guest）清空前一身份的 AI 回答、表单、筛选与 toast（套件 `verify:switch` 覆盖全部切换矩阵） |
| 抽屉只回不退 | 从记录抽屉里点开下一条记录时，来处留在底下；X / Esc 逐层弹回，而不是一关了之。预览抽屉可切全屏，偏好按类型记忆；表单抽屉始终保持书写宽度 |

## 数据与部署形态

- **前端**：单文件 `Waypoint-v1.html`（无构建）。服务器静态目录 `dist/` 里的 `index.html` 是它的副本——`npm run artefact` 同步两者。
- **服务器**：零依赖 Node（`server/`），一个端口同时服务静态页面与 `/api/*`；有自签证书时自动 https。
- **数据**：单文件 JSON + rev 文件；`WB_DATA_KEY` 可开静态加密；备份/恢复有专门脚本。
- **发布**：反向代理发布时，把公开域名加入 `server/published-origins.txt` 或设 `WB_ORIGIN_SUFFIX`（否则浏览器的 Origin 校验会拒绝保存）。

## 回归验证

```bash
npm run verify:all    # 28 套件 / 1427 checks
```

每个业务承诺都有对应套件盯着（权限、账本、AI 边界、防重、原子性、身份切换零残留……）。改任何东西之前，建议先看 `scripts/verify-all.mjs` 里的套件清单——那也是这个应用的行为规格。
