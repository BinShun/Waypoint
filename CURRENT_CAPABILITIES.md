# Waypoint v1 — 当前能力清单

> 本文档回答一个问题：**Version 1 今天实际上能做什么？** 全部条目以实际实现与回归验证为准（28 套件 / 1427 checks 背书）。凡未实现的能力，不在此列。

## 1. 工作台与导航

- **Today 屏**（登录首页）：我的焦点——逾期/到期的 Next Step（「Ours, past the date we set」）、等客户回复的事项（「The customer's court」）、按规则排序的今日视图
- **命令面板 palette**（快捷键唤起）：自然语言问答 + 快速跳转；常规问题（见 §9 规则直答）不经过模型，直接由应用规则作答
- **地址栏路由**：客户/机会/列表等视图有真实 URL，可前进后退、可分享链接、不可越权访问
- **移动端可用**：响应式布局（套件 live/routes 覆盖）
- **可读性**：默认主题小字 12px 起、行高放宽（专注阅读主题曾试用后按用户反馈移除）
- **预览抽屉统一体验**：机会 / 会议 / 人员记录点开即侧边抽屉预览；头部一键全屏/半屏切换，偏好按抽屉类型记忆（`wpDrwFull.<kind>`）；表单抽屉保持书写宽度不被切换
- **抽屉内下钻**：从记录抽屉里点开另一条记录（会议纪要里的人员、人员卡里的客户），上一条抽屉留在底下，X / Esc 逐层返回原处——返回而不是丢失

## 2. 客户管理（一本客户一本书）

- 客户档案：名称、行业、分类、 pains（痛点）、contacts（联系人）、owner
- **所有权即可见性**：每客户一个 owner（名字即权界）；owner 可把同事加进 `team` 或 `owners` 授予可见性；Manager 全景可见
- **confidential 客户**：对无权限者完全不可见；其数据**永不发送给模型**（服务端强制，403 + 明确文案）
- 客户详情屏：简报、机会、交互记录、Next Step、痛点、联系人、笔记多视图；简报六问区块可折叠并记住选择
- 动作：新建客户（BD/Admin）、编辑、记录交互（Interaction）
- 客户表格视图：表头吸顶、列头点击排序、筛选计数常显；长列表统一渐进渲染（Show N more · remaining）
- **版式体系**：容器三形态各司其职——卡片（内容面板）/ flow 流水（审计、Timeline、近期历史：无盒发丝线，月份头滚动吸顶）/ 表格（吸顶表头）；字阶五档（27 页题 / 15.5 节题 / 13 正文 / 12 辅 / 10.5 overline 组标）；Today 统计为单面板四列；流水行动作悬停浮现（键盘聚焦常显）

## 3. 机会管理（Opportunity）

- 机会挂在客户名下，带阶段（stage）、金额、来源
- **两套视图**：列表视图（全部机会）与单机会详情视图；表单无字段漂移（套件 opportunity 覆盖）
- 看板与列表均可按客户筛选（选择器 + 清除 chip + 计数）
- 机会与 MOM、Next Step 关联：从机会页可见其全部关联记录
- **交互记录 ↔ 机会多对多**：记一条交互时可关联多个机会——既可选已有（选择器 + 可移除行），也可就地新建（叠层抽屉，保存或取消都回到原表单，草稿不丢）
- AI 风险分析（见 §9）：每条风险必须**解释自己**，不出无解释的评分

## 4. Next Step 与 Tracker

- 每个下一步有**一个 owner**（负责人）和一个 **tracker**（跟踪人）——两向跟踪
- 状态：todo / in-progress / done；完成人记录（含 Non-Core Member 以名字完成）
- 到期日驱动 Today 屏的逾期/到期排序
- 服务端强制 step 的两角色规则（套件 steproles 覆盖）

## 4.5 People（人员与组织结构）

- 全局 People 页：按客户分组的联系人卡片，搜索 / 客户 / 立场筛选，分组渐进渲染
- **卡片只读**：点卡片打开记录抽屉预览；唯一的 Edit 在抽屉内（杜绝误触编辑）
- **Org map 模式**：Cards / Org map 切换，客户选择器驱动——选中客户即见其组织关系图（实线=见过，虚线=未见过），下方紧凑人员行同样开抽屉
- 客户详情 People 页保持主从面板（点人=侧面板，Edit 在面板内）
- **部门维度**：联系人记部门（客户级部门名册维护），列表按部门归类显示；部门缺失时由头衔推断并如实标注「from their title」；显示层对异常值兜底，任何字段都不会渲染成 `[object Object]`

## 5. 会议纪要（MOM）与交互记录

- **MOM 提取**：粘贴会议纪要原文 → AI 提取结构化清单（痛点 / 联系人 / 机会线索 / 下一步）→ **逐项勾选确认** → 才写入正式数据
- AI 只出草稿：**没有任何 AI 输出不经人的确认按压就直接落账**
- 交互记录（Interaction）：会议、邮件、电话等往来落账，关联客户/机会；一条交互可挂多个机会（见 §3）
- 会议抽屉展示该会产出的机会与下一步；MOM 落账的 step/opp 携带来源会议 id，回溯链条不断
- **MOM 摘要行**：AI 阅读纪要另产出 ≤220 字的 digest，专供时间线在列表里一行展示（长摘要不挤账本的账页）
- 笔记（Notes）：客户页可粘贴保存会议记录原文（§14——「从未变成会议的笔记」有专门提醒）

## 6. Timeline（一源真相）

- 每客户一条时间线：全部交互、步骤、机会事件按时间排列
- **不可删除**：历史只增不改，账本式追加（套件 truth 覆盖）
- 导入的数据保留原有关系，否则拒绝导入（套件 import 覆盖）

## 7. 日历视图

- Next Step 按到期日的月视图；可点进具体日期与事项（套件 calendar 覆盖）

## 8. Product Reference（产品目录）

- 内置 149 个可售服务、14 个分类（Compute / Database / AI …），含一句话定位
- 产品对账：AI 提到的任何产品**只能来自这个目录**（不发明产品，套件 ai 7i 覆盖）
- 产品可标记「谁在跟」（by）——Product SA 以名字参与，不自动联系、不自动指派

## 9. AI Business Copilot

**五个入口**（仅此五处，无通用聊天机器人）：

| 入口 | 位置 | 做什么 |
|---|---|---|
| My Work 聚焦 | Today 屏 | 总结我的焦点、建议下一步动作 |
| 客户简报 | 客户详情 | 生成/更新客户简报，提示机会线索（只提示，不自动建） |
| 机会分析 | 机会详情 | 风险与下一步建议，每条必须带解释 |
| MOM 提取 | 客户笔记 | 纪要 → 结构化清单 → 勾选确认 |
| palette 问答 | 全局 ⌘K | 上下文感知的问答 |

- **规则直答（零模型调用）**：等客户回复（waiting）、进展（progress）、停滞（stall）、已推进（moved）、痛点（pain/problem）、准备事项（prepare）、跟进缺失（follow-up）——这些问题由应用规则直接回答，不消耗 token
- **任务中心（AI Tasks）**：按压后立即显示 Working；离开页面返回结果仍在；任务表持久化可回看
- **防重复提交**：处理中的任务同键再按不产生新请求（UI 无发起钮 / HTTP 返回同 taskId / 模型请求计数恒定，三面防护）
- **权限**：AI 只能看到调用者本人可见的客户数据（服务端装配上下文，先校验后检索）；confidential 客户数据永不出门
- **端点可运维**：模型配置按「环境变量 → `data/ai.json`」顺序取；Admin 屏可在线改端点（动作写审计）。支持 OpenAI 兼容的推理型模型（返回 `reasoning_content`，当前默认预算能承载长推理链），示例见 `.env.example`
- **预算与超时**：Admin 可设预算上限；超时 90s、强制 fast 模型、失败有明确文案（不静默失败）
- **不发明事实**：产品只来自 Reference、不发明客户/人/机会/需求/商业事实；缺数据时诚实声明

## 10. 用户与权限

- 四角色：Admin / Manager / BD / SA（CAN 与 MUST-NOT 全矩阵由套件 roles 的 62 checks 覆盖）
- 密码 PBKDF2-HMAC-SHA256（150k 迭代、独立盐）；登录框按姓名匹配账号
- 会话 cookie + 「记住此设备」token；**登出即双侧吊销**（浏览器清除 + 服务器删除，套件 switch 覆盖），服务器重启亦全部失效——诚实版「记住」
- **身份切换零残留**：登出、换账号、进 Guest 模式时，前一身份的 AI 回答、dock 对话、表单、筛选状态、toast 全部清扫；深链（如他人分享的客户 URL）在新身份下渲染自己的书，不复活旧数据
- 失败登录锁定、API 限流（套件 security 覆盖）
- Admin 屏：用户管理（创建/角色）、AI 连接配置、预算设定
- **Manager 无写门**：新建客户、分析、粘贴等写入口对 Manager 不渲染（不只是禁用）

## 11. 数据与安全

- 单文件数据 `data/workbench.json` + 修订号（rev）：**原子保存**（写临时文件 + rename，断电不坏账）
- 乐观锁：保存带 baseRev，冲突走合并而非覆盖
- **保存结果如实上报**（套件 `verify:honesty`）：服务端回答的 `reverted`（角色无权修改的字段被退回）、`refused`（无权执行的删除）、`conflicts`（同事更新的行胜出）都不是静默的——状态条会说「Saved — 1 change not applied / 1 removal refused / Merged — 1 conflict」，并解释保留了谁的版本、你的那一份去了哪里
- **删除不谎报**：删除客户的响应被拒（会话过期 / 权限 / 断网）时，界面说「Not deleted — …」，该客户仍在账本里，删除信封保留到下一次保存重试
- **离开守卫**：存在未落库的改动时关闭/刷新页面会先问一句；页面干净时不打扰
- 可选静态加密（`seal` 模块，密钥来自 `WB_DATA_KEY`）
- 审计行：重大操作落审计（谁、何时、什么）
- 安全头：CSP / 防点击劫持 / 表单伪造防护 / Origin 校验（发布场景白名单 `server/published-origins.txt`）
- 无障碍：保存状态与 toast 均为 live region（`aria-live`），错误 toast 用 `role="alert"`；全站统一键盘焦点环（`:focus-visible`，深色导航用浅色环），下拉框补上焦点反馈；反代部署可设 `WB_TRUST_PROXY=1` 让限流按真实客户端 IP 计数（默认仍按 socket 地址）
- 窄屏不丢状态：≤880px 时保存状态胶囊从完整句子收缩为短词（Merged / Not applied / Refused / Offline），而不是只剩一个指示灯；「保存了但没全保存」在页面顶部另有可关闭的说明条

## 12. 无 AI 模式

不配模型时：**应用完全可用**（登录、客户、机会、步骤、MOM 手动记录、日历、palette 规则直答、保存链路全部照常），AI 入口显示「未连接」并指路 Admin 屏（冒烟套件 10/10 验证）。

## 已知边界（如实声明）

- AI 的「聪明程度」取决于所配模型；应用保证的是管道诚实（上下文正确、权限先于检索、输出先进草稿）
- 数据层是单文件 JSON——适合团队级（数十客户），不是数据库级扩展性
- 无多工作区/多租户：一台服务器一本书
